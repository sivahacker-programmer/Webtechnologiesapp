import 'package:flutter/material.dart';
import 'package:webtechnologiesapp/paper list.dart';

class Papers extends StatelessWidget {
  const Papers({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: PapersPage(),
    );
  }
}
