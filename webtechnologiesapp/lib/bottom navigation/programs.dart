import 'package:flutter/material.dart';
import 'package:webtechnologiesapp/programs list.dart';

class ProgramsMain extends StatelessWidget {
  const ProgramsMain({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: ProgramBar(),
    );
  }
}
